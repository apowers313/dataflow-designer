{
  "name": "@dataflow-designer/dataflow-url",
  "version": "0.4.0",
  "description": "HTTP source, sink, and through tools for getting dataflow data from URLs",
  "engines": {
    "node": ">=18"
  },
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "test": "yarn ts-mocha -p ./tsconfig.json test/**/*.test.ts",
    "build": "yarn tsc -p ./tsconfig.json",
    "lint": "yarn eslint --quiet lib/**/*.ts"
  },
  "author": "Adam Powers <apowers@ato.ms>",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "typedoc": {
    "entryPoint": "./index.ts",
    "readmeFile": "./README.md",
    "displayName": "Dataflow Url"
  },
  "dependencies": {
    "@dataflow-designer/dataflow-core": "^0.4.0",
    "@dataflow-designer/stupendous-parser": "^0.4.0",
    "handlebars": "^4.7.7",
    "undici": "^5.10.0"
  }
}
