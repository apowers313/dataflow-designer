{
  "name": "@dataflow-designer/dataflow-sample",
  "version": "0.3.0",
  "description": "A dataflow utility to only pass through every Nth chunk",
  "engines": {
    "node": ">=18"
  },
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "test": "ts-mocha -p ./tsconfig.json test/**/*.test.ts",
    "docs": "typedoc",
    "build": "tsc -p ./tsconfig.json",
    "lint": "npx eslint --quiet lib/**/*.ts"
  },
  "author": "Adam Powers <apowers@ato.ms>",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "typedoc": {
    "entryPoint": "./index.ts",
    "readmeFile": "./README.md",
    "displayName": "Dataflow Sample"
  },
  "dependencies": {
    "@dataflow-designer/dataflow-core": "^0.3.0"
  }
}
